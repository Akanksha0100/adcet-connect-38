# Local infrastructure: Postgres + MinIO. Run `docker compose up -d` to start.
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: adcet
      POSTGRES_PASSWORD: adcet
      POSTGRES_DB: adcet_alumni
    ports: ["5433:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U adcet -d adcet_alumni"]
      interval: 5s

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports: ["9000:9000", "9001:9001"]
    volumes: [miniodata:/data]

  createbuckets:
    image: minio/mc
    depends_on: [minio]
    volumes:
      - ./minio-cors.xml:/tmp/minio-cors.xml:ro
    entrypoint: >
      /bin/sh -c "
      sleep 3;
      mc alias set local http://minio:9000 minioadmin minioadmin;
      mc mb -p local/adcet-alumni || true;
      mc anonymous set download local/adcet-alumni || true;
      mc cors set local/adcet-alumni /tmp/minio-cors.xml || true;
      exit 0;
      "

volumes:
  pgdata:
  miniodata:
